import React from 'react';
import { noop } from 'lodash';
import { mount } from 'enzyme';

import SectionTree from './SectionTree';

const defaultProps = {

};

describe('DiscoverySideBarContainerPure', function() {
  it('should render a SectionTreeTab for each child of the current tree node', function() {

  });

  it('Should render a header tab with back button if >0 levels deep', function() {
    // const wrapper = mount(<SectionTree {...defaultProps} />);
  });
});
